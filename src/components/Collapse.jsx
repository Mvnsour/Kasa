import aboutList from '../datas/AboutList'

function Collapse() {}

export default Collapse


{
  /* mettre une condition si l contenu est du text, on l'affiche, 
et si c'est un array, on affiche une liste 
classe sur le parent */
}